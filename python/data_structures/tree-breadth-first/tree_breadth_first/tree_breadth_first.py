from .trees import BinaryTree, Node
from .queue import Queue


def breadth_first(self):
    """
    Arguments:
        self: takes in a tree as self.

    Returns:
        list: a list of the node values.
    """

    values_list = []

    # breadth_first_queue.enqueue(self.root)
    # dequeued_node = self.breadth_first_queue.dequeue()
    # values_list.append(dequeued_node.value)

    return values_list

